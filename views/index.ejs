<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Lisper</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Gloria+Hallelujah&family=Titan+One&display=swap" rel="stylesheet">
  <!-- Bootstrap CSS -->
  <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <!-- Your custom CSS file -->
  <link rel="stylesheet" type="text/css" href="styles/main.css">
  <style>
    body {
      font-family: 'Gloria Hallelujah', cursive;
      /*background-color: #f8f9fa;*/
      background-image: url('/images/bgprojet.jpg');
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;

    }

    h1 {
      text-align: center;
      margin-top: 50px;
      color: #ff6200;
      font-weight: 900;
    }

    form {
      max-width: 600px;
      margin: auto;
      background-color: #ffffff;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0px 0px 10px 0px #000000;
      margin-top: 30px;
    }

    label {
      display: block;
      margin-bottom: 10px;
    }

    button {
      background-color: #007bff;
      color: #ffffff;
      padding: 10px 20px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }

    .card {
      max-width: 600px;
      margin: auto;
      background-color: #ffffff;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0px 0px 10px 0px #000000;
      margin-top: 30px;
    }

    .user {
      text-align: center;
      margin-top: 20px;
      font-size: 1.8em;
      font-weight: 600;
      color: #6c757d;
    }
  </style>
</head>

<body>
  <h1>Get a Joke</h1>
  <form action="/submit" method="post">
    <div class="form-group">
      <label>
        <input type="radio" name="choice" value="any"> Any
      </label>
      <label>
        <input type="radio" name="choice" value="custom" id="customRadio"> Custom
      </label>
    </div>

    <div class="form-group">
      <label>
        <input type="checkbox" name="category" value="Programming"> Programming
      </label>
      <label>
        <input type="checkbox" name="category" value="Miscellaneous"> Misc
      </label>
      <label>
        <input type="checkbox" name="category" value="Dark"> Dark
      </label>
      <label>
        <input type="checkbox" name="category" value="Pun"> Pun
      </label>
    </div>

    <button type="submit" class="btn btn-primary">Get a Joke</button>
  </form>

  <div class="card">
    <p><%= setup %></p>
  </div>

  <p class="user"><%= delivery %></p>

  <script>
     // Get the radio button and checkbox elements
     const customRadio = document.getElementById('customRadio');
    const checkboxes = document.querySelectorAll('input[type="checkbox"]');
    const checkboxContainer = document.getElementById('checkboxContainer');

    // Add an event listener to the radio button
    customRadio.addEventListener('change', function () {
      // Enable or disable checkboxes based on the radio button's checked state
      checkboxes.forEach(checkbox => {
        checkbox.disabled = !customRadio.checked;
        if (!customRadio.checked) {
          checkbox.checked = false; // Uncheck checkboxes when disabling
        }
      });
    });
  </script>
  <!-- Bootstrap JS and Popper.js -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.0.7/dist/umd/popper.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.min.js"></script>
</body>

</html>
